apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: silverbullet-space
  namespace: silverbullet
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-ssd
  resources:
    requests:
      storage: 5Gi
